const form = document.querySelector('form');

form.addEventListener('submit',function (e) {
    e.preventDefault();

    const inputName  = e.target.querySelector('#name');
    const inputAge = e.target.querySelector('#age');

    const name = String(inputName.value);
    const age = String(inputAge.value);

    // console.log(age,name)


});


function inverseNameAge (name, age){
return null

}

let k = "batata";

